# pip alternative to environment.yml.
# The geospatial stack (cartopy, geopandas, rasterio) is much easier to
# install with conda; if pip struggles, use environment.yml instead.
numpy>=1.24
pandas>=2.0
xarray>=2023.6
netCDF4>=1.6
cftime
scipy>=1.10
dask[array]>=2023.5
PyYAML>=6.0
matplotlib>=3.7
statsmodels>=0.14
requests>=2.28
tabulate>=0.9

# optional but used by steps 11, 12, 15 and the map figures
geopandas>=0.13
regionmask>=0.9
rasterio>=1.3
cartopy>=0.22
isimip-client>=1.0

# development
pytest>=7.0
ruff>=0.1
